[project]
name = "rag-chunking-evaluation-analysis"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy>=2.0",
    "psycopg2-binary>=2.9",
    "pandas>=2.0",
    "numpy>=1.26",
    "scipy>=1.13",
    "matplotlib>=3.9",
    "seaborn>=0.13",
    "tabulate>=0.9",   # tabelas Markdown / LaTeX via pandas
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "jupyter>=1.0",
]
